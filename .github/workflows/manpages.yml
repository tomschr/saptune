name: ManpagePublish

on:
    push:
    pull_request:
    workflow_dispatch:

jobs:
    manpage:
        runs-on: ubuntu-latest

        steps:
            - name: Install pandoc
              run: sudo apt install -y pandoc

            - name: Check pandoc version
              run:  pandoc --version